Update PGE Orchestrator to the latest version.

Call the `pge_update` tool from the `pge-orchestrator` MCP server.
Show the output to the user.
If it says "Restart Claude Code to apply changes", tell the user to restart.
